add_library(omarchy-plugin-test-support STATIC src/test_support.cpp)
add_library(Omarchy::PluginTestSupport ALIAS omarchy-plugin-test-support)
omarchy_plugin_target_defaults(omarchy-plugin-test-support)
target_include_directories(omarchy-plugin-test-support PUBLIC include)

add_executable(omarchy-plugin-support-test tests/support_test.cpp)
omarchy_plugin_target_defaults(omarchy-plugin-support-test)
target_link_libraries(omarchy-plugin-support-test PRIVATE Omarchy::PluginTestSupport)
target_compile_definitions(omarchy-plugin-support-test PRIVATE
  WIRE_FIXTURE_ROOT="${CMAKE_CURRENT_SOURCE_DIR}/../fixtures/wire/v1"
)

if(TARGET omarchy-plugin-wire)
  target_link_libraries(omarchy-plugin-support-test PRIVATE omarchy-plugin-wire)
  target_compile_definitions(omarchy-plugin-support-test PRIVATE OMARCHY_TEST_HAS_WIRE=1)
endif()
if(TARGET omarchy-plugin-manifest-contract)
  target_link_libraries(omarchy-plugin-support-test PRIVATE omarchy-plugin-manifest-contract)
  target_compile_definitions(omarchy-plugin-support-test PRIVATE OMARCHY_TEST_HAS_MANIFEST=1)
endif()
if(TARGET omarchy-plugin-surface-contract)
  target_link_libraries(omarchy-plugin-support-test PRIVATE omarchy-plugin-surface-contract)
  target_compile_definitions(omarchy-plugin-support-test PRIVATE OMARCHY_TEST_HAS_SURFACE=1)
endif()

add_test(NAME plugin-test-support COMMAND omarchy-plugin-support-test)
set_tests_properties(plugin-test-support PROPERTIES LABELS "unit;property;fixtures" TIMEOUT 10)

if(TARGET omarchy-plugin-wire)
  add_executable(omarchy-plugin-malicious-peer-test tests/malicious_peer_test.cpp)
  omarchy_plugin_target_defaults(omarchy-plugin-malicious-peer-test)
  target_link_libraries(omarchy-plugin-malicious-peer-test PRIVATE
    Omarchy::PluginTestSupport
    omarchy-plugin-wire
  )
  add_test(NAME plugin-malicious-peer COMMAND omarchy-plugin-malicious-peer-test)
  set_tests_properties(plugin-malicious-peer PROPERTIES LABELS "integration;adversarial;linux" TIMEOUT 10)
endif()

option(PLUGIN_SECURITY_BUILD_FUZZERS "Build bounded libFuzzer targets" OFF)
if(PLUGIN_SECURITY_BUILD_FUZZERS)
  if(NOT CMAKE_CXX_COMPILER_ID MATCHES "Clang")
    message(FATAL_ERROR "PLUGIN_SECURITY_BUILD_FUZZERS requires Clang")
  endif()

  set(plugin_security_fuzzers)
  if(TARGET omarchy-plugin-wire)
    add_executable(omarchy-plugin-envelope-fuzzer fuzz/fuzz_envelope.cpp)
    target_link_libraries(omarchy-plugin-envelope-fuzzer PRIVATE omarchy-plugin-wire)
    list(APPEND plugin_security_fuzzers omarchy-plugin-envelope-fuzzer)
  endif()
  if(TARGET omarchy-plugin-manifest-contract)
    add_executable(omarchy-plugin-manifest-fuzzer fuzz/fuzz_manifest.cpp)
    target_link_libraries(omarchy-plugin-manifest-fuzzer PRIVATE omarchy-plugin-manifest-contract)
    list(APPEND plugin_security_fuzzers omarchy-plugin-manifest-fuzzer)
  endif()
  if(TARGET omarchy-plugin-surface-contract)
    add_executable(omarchy-plugin-render-fuzzer fuzz/fuzz_render.cpp)
    target_link_libraries(omarchy-plugin-render-fuzzer PRIVATE omarchy-plugin-surface-contract)
    list(APPEND plugin_security_fuzzers omarchy-plugin-render-fuzzer)
  endif()

  foreach(target IN LISTS plugin_security_fuzzers)
    omarchy_plugin_target_defaults(${target})
    target_compile_options(${target} PRIVATE -fsanitize=fuzzer,address,undefined -fno-omit-frame-pointer)
    target_link_options(${target} PRIVATE -fsanitize=fuzzer,address,undefined)
  endforeach()
  add_custom_target(plugin-security-fuzzers DEPENDS ${plugin_security_fuzzers})
endif()

#!/bin/bash

# omarchy:summary=Review, inspect, grant, deny, or revoke secure plugin permissions
# omarchy:group=plugin
# omarchy:args=<list|diff|review|grant|deny|revoke> [options]
# omarchy:examples=omarchy plugin permission list --format human | omarchy plugin permission review --help

set -euo pipefail

exec omarchy-plugin-permission-store "$@"
